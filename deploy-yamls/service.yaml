apiVersion: v1
kind: Service
metadata:
  labels:
    app: bonuscustomerprofile
    environment: Development
  name: bonuscustomerprofile
  namespace: services
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 5000
  selector:
    app: bonuscustomerprofile
  sessionAffinity: None
  type: ClusterIP
